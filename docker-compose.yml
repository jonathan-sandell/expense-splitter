services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: expense_splitter
      POSTGRES_USER: expense_splitter
      POSTGRES_PASSWORD: expense_splitter
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: